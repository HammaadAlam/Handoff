/**
 * Poppins — loaded in App.tsx via @expo-google-fonts/poppins (names must match useFonts keys).
 * Use explicit fontFamily instead of fontWeight so weights render correctly on iOS.
 */
export const fonts = {
  regular: 'Poppins_400Regular',
  medium: 'Poppins_500Medium',
  semiBold: 'Poppins_600SemiBold',
  bold: 'Poppins_700Bold',
  extraBold: 'Poppins_800ExtraBold',
} as const;
