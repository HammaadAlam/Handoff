/**
 * Roboto — loaded in App.tsx via @expo-google-fonts/roboto (names must match useFonts keys).
 * Use explicit fontFamily instead of fontWeight so weights render correctly on iOS.
 */
export const fonts = {
  regular: 'Roboto_400Regular',
  medium: 'Roboto_500Medium',
  semiBold: 'Roboto_600SemiBold',
  bold: 'Roboto_700Bold',
  extraBold: 'Roboto_800ExtraBold',
} as const;
